name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Assemble static site
        shell: bash
        run: |
          mkdir -p public/demo public/assets public/schemas public/try
          cp site/index.html site/styles.css site/404.html site/social-preview.png public/
          cp src/frontiertrials/web/index.html src/frontiertrials/web/styles.css src/frontiertrials/web/app.js public/try/
          cp docs/assets/*.svg public/assets/
          cp examples/demo/reports/trial-report.html public/demo/
          cp examples/demo/packets/reviewer-one.html public/demo/
          cp examples/demo/reports/ranking.csv public/demo/
          cp examples/demo/reports/protocol.md public/demo/
          cp examples/demo/reports/adjudication.md public/demo/
          cp examples/demo/reports/adjudication.json public/demo/
          cp schemas/*.json public/schemas/
          touch public/.nojekyll
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: public
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
